<h2><%= @cocktail.name %></h2>


<% @cocktail.doses.each do |dose| %>
    <%= dose.id %> <%= dose.description %> - <%= dose.ingredient.name %> - <%= link_to "x", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>

<%= link_to 'Add new dose', new_cocktail_dose_path(@cocktail) %>